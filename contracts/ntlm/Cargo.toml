[package]
name = "ntlm_contract"
version = "0.1.0"
edition = "2021"

[dependencies]
solana-program = "=2.0.3"
ntlm-hash = "0.1"  # Para generar el NT hash (MD4)
hex = "0.4"

[lib]
name = "ntlm_contract"
crate-type = ["cdylib", "lib"]
